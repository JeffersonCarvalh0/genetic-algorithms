# KSP

## Build Instructions

Clone the project and `cd` to its root directory. Then:

```
    mkdir build && cd build
    cmake ..
    make
```

After that, you can execute the program using `./main`. <br>
Note that [CMake](https://cmake.org) is required in order to build the project.
